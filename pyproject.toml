[project]
name = "lab-teleport"
version = "0.0.3"
requires-python = "==3.14.*"
dependencies = ["ansible", "cryptography", "jmespath", "kubernetes"]

[dependency-groups]
dev = ["ansible-lint"]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
